# Generated Cmake Pico project file

cmake_minimum_required(VERSION 3.13)

set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# Initialise pico_sdk from installed location
# (note this can come from environment, CMake cache etc)

# == DO NOT EDIT THE FOLLOWING LINES for the Raspberry Pi Pico VS Code Extension to work ==
if(WIN32)
    set(USERHOME $ENV{USERPROFILE})
else()
    set(USERHOME $ENV{HOME})
endif()
set(sdkVersion 2.2.0)
set(toolchainVersion 14_2_Rel1)
set(picotoolVersion 2.2.0)
set(picoVscode ${USERHOME}/.pico-sdk/cmake/pico-vscode.cmake)
if (EXISTS ${picoVscode})
    include(${picoVscode})
endif()
# ====================================================================================
if(NOT DEFINED PICO_BOARD)
    set(PICO_BOARD pico2 CACHE STRING "Board type")
endif()

# Pull in Raspberry Pi Pico SDK (must be before project)
include(pico_sdk_import.cmake)

project(Touchord C CXX ASM)

# Initialise the Raspberry Pi Pico SDK
pico_sdk_init()

# Add executable. Default name is the project name, version 0.1

add_executable(Touchord
                        src/Touchord.c
                        src/Debug.c
                        src/Globals.c
                        src/usb_descriptors.c
                        src/Modes/Compose.c
                        src/Modes/Strum.c
                        src/Modes/Settings.c
                        src/Modes/Perform.c
                        src/Modes/Omni.c
                        src/Modes/Drum.c
                        src/Notes/Note.c
                        src/Rendering/Graphics.c
                        src/IO/Midi.c
                        src/IO/Output.c
                        src/Audio/Pcm5102Audio.c
                        src/Synth/AmyEngine.c
                        src/Synth/AmyPlatformStubs.c
                        src/Data/Parser.c
                        src/thirdparty/tiny-json.c
                        src/thirdparty/Fonts.c
                        src/thirdparty/ssd1306.c
                        amy/src/algorithms.c
                        amy/src/amy.c
                        amy/src/api.c
                        amy/src/custom.c
                        amy/src/delay.c
                        amy/src/envelope.c
                        amy/src/filters.c
                        amy/src/instrument.c
                        amy/src/interp_partials.c
    amy/src/log2_exp2.c
    amy/src/midi_mappings.c
    amy/src/oscillators.c
    amy/src/parse.c
                        amy/src/patches.c
                        amy/src/pcm.c
                        amy/src/sequencer.c
                        amy/src/transfer.c)

pico_generate_pio_header(Touchord ${CMAKE_CURRENT_LIST_DIR}/src/Audio/tc_audio_i2s.pio)
                        
                        
pico_set_program_name(Touchord "Touchord")
pico_set_program_version(Touchord "0.1")

# The firmware owns a custom TinyUSB CDC+MIDI descriptor. Keep Pico stdio off so
# it does not compete with that descriptor or claim GPIO0/1 for UART debug.
pico_enable_stdio_uart(Touchord 0)
pico_enable_stdio_usb(Touchord 0)

# Add the standard library to the build
target_link_libraries(Touchord
        pico_stdlib
        pico_multicore
        tinyusb_device
        tinyusb_board)

# Add the standard include files to the build
target_include_directories(Touchord PRIVATE
        ${CMAKE_CURRENT_LIST_DIR}/src
        ${CMAKE_CURRENT_LIST_DIR}/src/thirdparty
        ${CMAKE_CURRENT_LIST_DIR}/amy/src
)

target_compile_definitions(Touchord PRIVATE
        PICO_ON_DEVICE=1
        AMY_NO_MINIAUDIO=1
)

# Add any user requested libraries
target_link_libraries(Touchord 
        hardware_spi
        hardware_i2c
        hardware_dma
        hardware_pio
        hardware_clocks
        m
        
        )

pico_add_extra_outputs(Touchord)
